apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app: postgres
spec:
  selector:
    app: postgres               # Pekar på poddens label
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  type: ClusterIP              # bara internt i klustret